name: Build Windows

permissions:
  contents: read

on:
  push:
    paths:
      - 'client/windows/**'
      - '.github/workflows/build-windows.yml'
  workflow_dispatch:
  workflow_call:

env:
  SDL_REF: release-3.4.10

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build Terminal Client (MinGW)
        shell: bash
        run: |
          cd client/windows/
          g++ -std=c++17 -O2 -Wall ns-gamepad.cpp -o ns-gamepad.exe -static \
            -lws2_32 -lxinput -lwinmm -luser32 -lhid -lsetupapi

      - name: Setup MSVC
        uses: TheMrMilchmann/setup-msvc-dev@v4
        with:
          arch: x64

      - name: Fetch SDL3
        shell: pwsh
        run: |
          git clone --depth 1 --branch $env:SDL_REF https://github.com/libsdl-org/SDL.git _deps/SDL

      - name: Build GUI Client (MSVC + static SDL3)
        shell: pwsh
        run: |
          cd client/windows

          @'
          cmake_minimum_required(VERSION 3.20)
          project(ns_gui_sdl3 LANGUAGES C CXX RC)

          set(CMAKE_CXX_STANDARD 17)
          set(CMAKE_CXX_STANDARD_REQUIRED ON)
          set(CMAKE_CXX_EXTENSIONS OFF)

          if(MSVC)
            set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
          endif()

          set(SDL_SHARED OFF CACHE BOOL "" FORCE)
          set(SDL_STATIC ON CACHE BOOL "" FORCE)
          set(SDL_TESTS OFF CACHE BOOL "" FORCE)
          set(SDL_TEST_LIBRARY OFF CACHE BOOL "" FORCE)
          set(SDL_EXAMPLES OFF CACHE BOOL "" FORCE)
          set(SDL_DISABLE_INSTALL ON CACHE BOOL "" FORCE)

          add_subdirectory("${CMAKE_CURRENT_LIST_DIR}/../../_deps/SDL" SDL-build EXCLUDE_FROM_ALL)

          add_executable(ns-gui WIN32
            ns-gui.cpp
            ns-gui.rc
          )

          target_compile_definitions(ns-gui PRIVATE
            UNICODE
            _UNICODE
            WIN32_LEAN_AND_MEAN
            NOMINMAX
            _CRT_SECURE_NO_WARNINGS
            SDL_MAIN_HANDLED
            NS_SDL3_STATIC
            NS_LINK_SDL3_MANUALLY
          )

          if(MSVC)
            target_compile_options(ns-gui PRIVATE /W3 /O2 /EHsc)
          endif()

          if(TARGET SDL3::SDL3-static)
            set(NS_SDL3_TARGET SDL3::SDL3-static)
          else()
            set(NS_SDL3_TARGET SDL3::SDL3)
          endif()

          target_link_libraries(ns-gui PRIVATE
            ${NS_SDL3_TARGET}
            ws2_32
            comctl32
            comdlg32
            winmm
            user32
            kernel32
            gdi32
            advapi32
            shell32
            ole32
            oleaut32
            uuid
            version
            setupapi
            imm32
          )

          set_target_properties(ns-gui PROPERTIES
            RUNTIME_OUTPUT_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}"
            RUNTIME_OUTPUT_DIRECTORY_RELEASE "${CMAKE_CURRENT_LIST_DIR}"
            RUNTIME_OUTPUT_DIRECTORY_RELWITHDEBINFO "${CMAKE_CURRENT_LIST_DIR}"
            RUNTIME_OUTPUT_DIRECTORY_MINSIZEREL "${CMAKE_CURRENT_LIST_DIR}"
          )
          '@ | Set-Content -Encoding UTF8 CMakeLists.txt

          cmake -S . -B build-sdl3 -G "Visual Studio 17 2022" -A x64 `
            -DCMAKE_BUILD_TYPE=Release `
            -DCMAKE_MSVC_RUNTIME_LIBRARY=MultiThreaded

          cmake --build build-sdl3 --config Release --target ns-gui --parallel

          if (Test-Path ".\SDL3.dll") {
            throw "SDL3.dll was produced next to ns-gui.exe; expected a single statically-linked exe."
          }

      - uses: actions/upload-artifact@v6
        with:
          name: windows-gamepad
          path: client/windows/ns-gamepad.exe

      - uses: actions/upload-artifact@v6
        with:
          name: windows-gui
          path: client/windows/ns-gui.exe
